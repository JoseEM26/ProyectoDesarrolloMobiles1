<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="16dp"
    android:layout_marginTop="12dp"
    android:layout_marginEnd="16dp"
    android:layout_marginBottom="12dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    app:cardBackgroundColor="@color/BackGround"
    app:cardCornerRadius="20dp"
    app:cardElevation="6dp"
    app:cardUseCompatPadding="true"
    app:strokeColor="@color/azul_suave"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="20dp">

        <!-- Avatar dinámico según tipo de usuario -->
        <ImageView
            android:id="@+id/ivAvatar"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_marginEnd="16dp"
            android:background="@drawable/circle_avatar_background"
            android:contentDescription="@string/user_type_student"
            android:scaleType="centerInside"
            android:src="@drawable/ic_student"
            app:tint="@color/azul_profundo" />

        <!-- Columna de datos del usuario -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/txtUsuariosNombre"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:fontFamily="sans-serif-medium"
                android:maxLines="1"
                android:text="@string/user_name"
                android:textColor="@color/azul_oscuro"
                android:textSize="18sp"
                android:textStyle="bold"
                tools:text="@string/text_juan_perez_lopez" />

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/txtUsuariosTipo"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:fontFamily="sans-serif"
                android:text="@string/user_type_student"
                android:textColor="@color/azul_profundo"
                android:textSize="14sp"
                tools:text="@string/user_type_student" />

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/txtUsuariosSede"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:fontFamily="sans-serif"
                android:text="@string/user_details"
                android:textColor="@color/gris_oscuro"
                android:textSize="12sp"
                tools:text="@string/text_sede_lima" />

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/txtUsuariosCreated"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:fontFamily="sans-serif"
                android:text="@string/user_created"
                android:textColor="@color/gris_claro"
                android:textSize="12sp"
                tools:text="@string/text_creado" />
        </LinearLayout>

        <!-- Estado del usuario -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="12dp"
            android:gravity="center"
            android:orientation="vertical">

            <ImageView
                android:id="@+id/ivStatus"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_marginBottom="4dp"
                android:contentDescription="@string/user_status"
                android:src="@drawable/ic_active"
                app:tint="@color/verde_exito" />

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/txtUsuariosActivo"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif-medium"
                android:text="@string/user_active"
                android:textColor="@color/verde_exito"
                android:textSize="12sp"
                android:textStyle="bold"
                tools:text="@string/user_active" />
        </LinearLayout>

        <!-- Botón de opciones -->
        <ImageButton
            android:id="@+id/btnMenu"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="center_vertical"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/menu_action"
            android:scaleType="fitCenter"
            android:src="@drawable/baseline_more_vert_24"
            app:tint="@color/azul_oscuro" />

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>